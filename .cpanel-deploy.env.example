# cPanel Auto-Deploy Configuration
# Copy this file to .cpanel-deploy.env and fill in your values.
# DO NOT commit .cpanel-deploy.env to git (it contains secrets).

# --- cPanel API (for creating database & user) ---
# Your cPanel host (e.g. box123.host.com or yourdomain.com - check your host's welcome email)
CPANEL_HOST=your-cpanel-host.com

# cPanel username (your hosting account login)
CPANEL_USER=your_cpanel_username

# Either use an API token (recommended) OR your cPanel password
# Create token: cPanel → Security → Manage API Tokens → Create
CPANEL_TOKEN=your_api_token_here
# CPANEL_PASS=your_cpanel_password

# --- FTP (for uploading files) ---
# Usually same as cPanel host, or ftp.yourdomain.com
FTP_HOST=your-cpanel-host.com
FTP_USER=your_cpanel_username
FTP_PASS=your_cpanel_password

# --- Your site ---
# Your domain (e.g. https://yourdomain.com - no trailing slash)
DOMAIN=https://yourdomain.com

# --- Database (optional - script can create these via API) ---
# Short names; cPanel adds a prefix. Script uses these if creating via API.
DB_NAME=inventory_sales
DB_USER=inventory_user
DB_PASS=change_this_strong_password

# --- Optional: skip API (if you already created DB manually) ---
# Set to 1 to skip creating database/user via API (you did it in cPanel)
# SKIP_DB_CREATE=0

# --- Optional: skip SQL import ---
# Set to 1 to skip running schema import (e.g. you already imported)
# SKIP_DB_IMPORT=0
